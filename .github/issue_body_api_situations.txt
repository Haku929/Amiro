## 目的
その日のシチュエーション 3 件を返す API を用意する。

## やること
- `GET /api/ai/situations` を実装する。クエリ `?date=YYYY-MM-DD`（省略時は当日 UTC）。日付をシードに DB のシチュエーションリストから 3 件を選び、同一日は同じ 3 件を返す。`{ situations: string[] }`（長さ 3）。AI は使わない。

## 依存
シチュエーションの保存（DB）

## 参照
仕様書 5.6 シチュエーション、6. 実装 TODO リスト → AI 連携 [P1]
